services:
  redis:
    image: redis:latest
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 30s
      timeout: 5s
      retries: 5
    ports:
      - '6379:6379'
    networks:
      - backend

#  eureka-server:
#    image: evseevdmitry/dobri-pet-eureka-server:0.0.1-SNAPSHOT
#    healthcheck:
#      test: [ "CMD", "curl", "-f", "http://eureka-server:8761/actuator/health" ]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#    ports:
#      - '8761:8761'
#    networks:
#      - backend
#    depends_on:
#      kafka:
#        condition: service_healthy
#      redis:
#        condition: service_healthy
#    environment:
#      - SERVER_PORT=8761
#      - eureka.instance.preferIpAddress=true

#  hash-generator-db:
#    image: postgres:16.2
#    healthcheck:
#      test: [ "CMD", "pg_isready" ]
#      interval: 30s
#      timeout: 5s
#      retries: 5
#    ports:
#      - '5433:5433'
#    networks:
#      - backend
#    restart: unless-stopped
#    volumes:
#      - ./hash-generator/infrastructure/db/create_db.sql:/docker-entrypoint-initdb.d/init_db.sql
#      #        for production
#      #      - hash-generator-pgdata:/var/lib/postgresql/data
#    environment:
#      POSTGRES_USER: ${HASH_GENERATOR_DATASOURCE_USERNAME}
#      POSTGRES_PASSWORD: ${HASH_GENERATOR_DATASOURCE_PASSWORD}
#      PGPORT: ${HASH_GENERATOR_DATASOURCE_PORT}

#  hash-generator-service:
#    image: evseevdmitry/pastebin-hash-generator-service:0.0.1-SNAPSHOT
#    healthcheck:
#      test: ["CMD", "curl", "-f", "http://hash-generator-service:8082/actuator/health"]
#      interval: 30s
#      timeout: 5s
#      retries: 5
#    ports:
#      - '8082:8082'
#    networks:
#      - backend
#    depends_on:
#      api-gateway:
#        condition: service_healthy
#      hash-generator-db:
#        condition: service_healthy
#    environment:
##       APPLICATION
#      - SERVER_PORT=8082
#       
##       DATASOURCE
#      - DATASOURCE_URL=${HASH_GENERATOR_DATASOURCE_URL}
#      - DATASOURCE_USERNAME=${HASH_GENERATOR_DATASOURCE_USERNAME}
#      - DATASOURCE_PASSWORD=${HASH_GENERATOR_DATASOURCE_PASSWORD}
#      - HASH_SEED_SEQ_NAME=${HASH_SEED_SEQ_NAME}
#
##       REDIS
#      - REDIS_HOST=${REDIS_HOST}
#      - REDIS_PORT=${REDIS_PORT}
#      - REDIS_DATABASE=${HASH_GENERATOR_REDIS_DATABASE}
#      - REDIS_PASSWORD=${REDIS_PASSWORD}
#
##       HASH PROPERTIES
#      - HASH_CASH_SIZE=${HASH_CASH_SIZE}
#      - HASH_CASH_REDIS_KEY_NAME=${HASH_CASH_REDIS_KEY_NAME}
#
##       EUREKA
##      - eureka.client.serviceUrl.defaultZone=http://eureka-server:8761/eureka

networks:
  backend:
    driver: bridge

# for production
#volumes:
#  hash-generator-pgdata